import { createSignal, onMount } from "solid-js"; import { enable, disable } from "@tauri-apps/plugin-autostart"; import { getStoreValue, setStoreValue } from "@/config/store";
type Config={pomodoroTimeSeconds:number;shortBreakTimeSeconds:number;longBreakTimeSeconds:number;numberOfRounds:number};const defaults:Config={pomodoroTimeSeconds:1500,shortBreakTimeSeconds:300,longBreakTimeSeconds:900,numberOfRounds:4};
const Toggle=(p:{on:boolean;set:(x:boolean)=>void})=><button role="switch" aria-checked={p.on} class={`switch ${p.on?"on":""}`} onClick={()=>p.set(!p.on)}><i/></button>;
const Step=(p:{value:number;set:(x:number)=>void;min?:number;unit?:string})=><div class="stepper"><button onClick={()=>p.set(Math.max(p.min||1,p.value-1))}>−</button><span>{p.value}{p.unit}</span><button onClick={()=>p.set(p.value+1)}>＋</button></div>;
export default function Settings(){const [close,setClose]=createSignal(false),[minimized,setMinimized]=createSignal(false),[particles,setParticles]=createSignal(true),[notify,setNotify]=createSignal(true),[remind,setRemind]=createSignal(true),[volume,setVolume]=createSignal(70),[config,setConfig]=createSignal(defaults);onMount(async()=>{setClose((await getStoreValue<boolean>("closeToTray"))||false);setMinimized((await getStoreValue<boolean>("startMinimized"))||false);setParticles((await getStoreValue<boolean>("ambientParticles"))??true);setNotify((await getStoreValue<boolean>("notifications"))??true);setRemind((await getStoreValue<boolean>("reminders"))??true);setVolume((await getStoreValue<number>("soundVolume"))||70);setConfig((await getStoreValue<Config>("pomodoro-settings"))||defaults)});const save=async()=>{await Promise.all([setStoreValue("closeToTray",close()),setStoreValue("startMinimized",minimized()),setStoreValue("ambientParticles",particles()),setStoreValue("notifications",notify()),setStoreValue("reminders",remind()),setStoreValue("soundVolume",volume()),setStoreValue("pomodoro-settings",config())])};return <section class="page"><div class="toolbar"><div><h1>Settings</h1><p class="subtitle">Make Focus Clock work your way</p></div></div><div class="settings-grid"><section class="panel"><h2>General</h2><div class="setting"><div class="copy"><strong>Launch at startup</strong><small>Start Focus Clock when Windows starts</small></div><Toggle on={false} set={async v=>v?enable():disable()}/></div><div class="setting"><div class="copy"><strong>Minimize to tray</strong><small>Keep running in the background</small></div><Toggle on={close()} set={v=>setClose(v)}/></div><div class="setting"><div class="copy"><strong>Start minimized</strong><small>Launch directly to system tray</small></div><Toggle on={minimized()} set={v=>setMinimized(v)}/></div></section><section class="panel"><h2>Appearance</h2><div class="setting"><div class="copy"><strong>Ambient texture</strong><small>Show restrained visual texture</small></div><Toggle on={particles()} set={v=>setParticles(v)}/></div><div class="setting"><div class="copy"><strong>Background intensity</strong><small>Adjust background visibility</small></div><input class="slider" type="range"/></div></section><section class="panel"><h2>Notifications</h2><div class="setting"><div class="copy"><strong>Timer complete</strong><small>Show a notification when a timer ends</small></div><Toggle on={notify()} set={v=>setNotify(v)}/></div><div class="setting"><div class="copy"><strong>Session reminders</strong><small>Gentle reminders to keep you on track</small></div><Toggle on={remind()} set={v=>setRemind(v)}/></div><div class="setting"><div class="copy"><strong>Sound volume</strong><small>Adjust notification volume</small></div><input class="slider" type="range" value={volume()} onInput={e=>setVolume(Number(e.currentTarget.value))}/></div></section><section class="panel"><h2>Timer defaults</h2><div class="setting"><div class="copy"><strong>Focus</strong><small>Default focus session length</small></div><Step value={config().pomodoroTimeSeconds/60} unit=" min" set={v=>setConfig({...config(),pomodoroTimeSeconds:v*60})}/></div><div class="setting"><div class="copy"><strong>Short break</strong><small>Default short break length</small></div><Step value={config().shortBreakTimeSeconds/60} unit=" min" set={v=>setConfig({...config(),shortBreakTimeSeconds:v*60})}/></div><div class="setting"><div class="copy"><strong>Long break</strong><small>Default long break length</small></div><Step value={config().longBreakTimeSeconds/60} unit=" min" set={v=>setConfig({...config(),longBreakTimeSeconds:v*60})}/></div><div class="setting"><div class="copy"><strong>Sessions</strong><small>Default number of sessions</small></div><Step value={config().numberOfRounds} set={v=>setConfig({...config(),numberOfRounds:v})}/></div></section></div><div class="settings-footer"><button class="button" onClick={()=>setConfig(defaults)}>Reset defaults</button><button class="button primary" onClick={save}>Save changes</button></div></section>}
