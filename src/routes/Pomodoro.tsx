import { createSignal, onCleanup, onMount, For } from "solid-js"; import { Play, Pause, RotateCcw } from "lucide-solid"; import { getStoreValue, setStoreValue } from "@/config/store";
type Config={pomodoroTimeSeconds:number;shortBreakTimeSeconds:number;longBreakTimeSeconds:number;numberOfRounds:number};const defaults:Config={pomodoroTimeSeconds:1500,shortBreakTimeSeconds:300,longBreakTimeSeconds:900,numberOfRounds:4};const fmt=(s:number)=>`${Math.floor(s/60).toString().padStart(2,"0")}:${(s%60).toString().padStart(2,"0")}`;
export default function Pomodoro(){const [config,setConfig]=createSignal(defaults),[mode,setMode]=createSignal<"focus"|"short"|"long">("focus"),[left,setLeft]=createSignal(defaults.pomodoroTimeSeconds),[running,setRunning]=createSignal(false),[task,setTask]=createSignal(""),[sessions,setSessions]=createSignal(0);let end=0,tick:number|undefined;const duration=()=>mode()==="focus"?config().pomodoroTimeSeconds:mode()==="short"?config().shortBreakTimeSeconds:config().longBreakTimeSeconds;const choose=(m:typeof mode extends ()=>infer T?T:never)=>{clearInterval(tick);setRunning(false);setMode(m);setLeft(m==="focus"?config().pomodoroTimeSeconds:m==="short"?config().shortBreakTimeSeconds:config().longBreakTimeSeconds)};const update=()=>{const n=Math.max(0,Math.ceil((end-Date.now())/1000));setLeft(n);if(!n){clearInterval(tick);setRunning(false);if(mode()==="focus"){const next=sessions()+1;setSessions(next);choose(next%config().numberOfRounds===0?"long":"short")}else choose("focus")}};onMount(async()=>{const c=(await getStoreValue<Config>("pomodoro-settings"))||defaults;setConfig(c);setLeft(c.pomodoroTimeSeconds);setTask((await getStoreValue<string>("pomodoro-task"))||"")});onCleanup(()=>clearInterval(tick));const toggle=()=>{if(running()){clearInterval(tick);setRunning(false)}else{end=Date.now()+left()*1000;tick=window.setInterval(update,250);setRunning(true)}};const label=()=>mode()==="focus"?"Focus session":mode()==="short"?"Short break":"Long break";return <section class="page pomodoro"><div><div class="tabs"><button class={mode()==="focus"?"active":""} onClick={()=>choose("focus")}>Focus<br/>{config().pomodoroTimeSeconds/60}</button><button class={mode()==="short"?"active":""} onClick={()=>choose("short")}>Short break<br/>{config().shortBreakTimeSeconds/60}</button><button class={mode()==="long"?"active":""} onClick={()=>choose("long")}>Long break<br/>{config().longBreakTimeSeconds/60}</button></div><div class="clock-face"><div class="clock-content"><p class="eyebrow">{label()}</p><div class="time">{fmt(left())}</div><p class="date">{mode()==="focus"?"Stay with one task":"Take a breath"}</p></div></div><div class="pomodoro-actions"><button class="button primary" onClick={toggle}>{running()?<Pause/>:<Play fill="currentColor"/>}{running()?"Pause":"Start"}</button><button class="button" onClick={()=>{clearInterval(tick);setRunning(false);setLeft(duration())}}><RotateCcw/>Reset</button></div></div><aside class="panel today"><h2>Today</h2><div class="session-count">{sessions()} <span class="muted">/ {config().numberOfRounds} <small>sessions</small></span></div><div class="dots"><For each={Array.from({length:config().numberOfRounds})}>{(_,i)=><i class={i()<sessions()?"done":""}/>}</For></div><hr class="section-rule"/><span class="muted">Focus time</span><h2>{Math.round(sessions()*config().pomodoroTimeSeconds/60)} min</h2><hr class="section-rule"/><label class="muted">Current task</label><input class="task-input" value={task()} onInput={e=>{setTask(e.currentTarget.value);void setStoreValue("pomodoro-task",e.currentTarget.value)}} placeholder="What are you working on?"/><hr class="section-rule"/><strong>Next: <span class="muted">{mode()==="focus"?"Short break":"Focus"}</span></strong></aside></section>}
