import { createSignal, onCleanup, onMount } from "solid-js"; import { useParams, A } from "@solidjs/router"; import { Play, Pause, RotateCcw } from "lucide-solid"; import { getStoreValue, setStoreValue } from "@/config/store";
type TimerData={id:number;title:string;duration:number}; const fmt=(s:number)=>`${Math.floor(s/60).toString().padStart(2,"0")}:${(s%60).toString().padStart(2,"0")}`;
export default function TimerMain(){const id=Number(useParams().id),[timer,setTimer]=createSignal<TimerData|null>(null),[left,setLeft]=createSignal(0),[running,setRunning]=createSignal(false);let end=0,tick:number|undefined;const update=()=>{const next=Math.max(0,Math.ceil((end-Date.now())/1000));setLeft(next);if(!next){setRunning(false);clearInterval(tick);void complete()}};const complete=async()=>{const t=timer();if(!t)return;const history=(await getStoreValue<any[]>("timer-history"))||[];void setStoreValue("timer-history",[{title:t.title,duration:t.duration,at:"Just now"},...history].slice(0,20))};onMount(async()=>{const found=((await getStoreValue<TimerData[]>("timers"))||[]).find(t=>t.id===id);setTimer(found||null);setLeft(found?.duration||0)});onCleanup(()=>clearInterval(tick));const toggle=()=>{if(running()){clearInterval(tick);setRunning(false)}else if(left()>0){end=Date.now()+left()*1000;tick=window.setInterval(update,250);setRunning(true)}};return <section class="page home"><div class="clock-face"><div class="clock-content"><p class="eyebrow">Countdown</p><div class="time">{fmt(left())}</div><p class="date">{timer()?.title||"Timer not found"}</p></div></div><div class="pomodoro-actions"><button class="button primary" onClick={toggle}>{running()?<Pause/>:<Play fill="currentColor"/>}{running()?"Pause":"Start"}</button><button class="button" onClick={()=>{clearInterval(tick);setRunning(false);setLeft(timer()?.duration||0)}}><RotateCcw/>Reset</button></div><A class="muted" href="/timers">Back to timers</A></section>}
